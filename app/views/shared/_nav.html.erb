<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container px-4 px-lg-5">
    <a class="navbar-brand" href="#!">item list</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
        <% if current_user %>
          <li class="nav-item"><%= link_to 'items', items_path, class: 'nav-link' %>
          <% if current_user.admin? %>
            <li class="nav-item"><%= link_to 'users', admin_users_path, class: 'nav-link' %>
          <% end %>
          <li class="nav-item"><%= link_to 'logout', logout_path, data: { turbo_method: :delete }, class: 'nav-link' %>
        <% else %>
          <li class="nav-item"><%= link_to 'login', login_path, class: 'nav-link' %>
        <% end %>
      </ul>

      <form class="d-flex">
        <button class="btn btn-outline-dark" type="submit">
          <i class="bi-cart-fill me-1"></i>
          Cart
          <span class="badge bg-dark text-white ms-1 rounded-pill">0</span>
        </button>
      </form>
    </div>
  </div>
</nav>
